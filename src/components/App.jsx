function App() {
  function handleSubmit(event) {
    event.preventDefault();
    // submit to API
  }
  return (
    <form
      onSubmit={handleSubmit}
    >
    </form>
  );
}

export default App;
